apiVersion: v1
kind: Service
metadata:
  name: {{ include "tinyolly.otlpReceiver.name" . }}
  labels:
    {{- include "tinyolly.otlpReceiver.labels" . | nindent 4 }}
  {{- with .Values.otlpReceiver.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.otlpReceiver.service.type }}
  ports:
  - name: grpc
    port: {{ .Values.otlpReceiver.service.port }}
    targetPort: {{ .Values.otlpReceiver.service.targetPort }}
    protocol: TCP
  selector:
    {{- include "tinyolly.otlpReceiver.selectorLabels" . | nindent 4 }}
