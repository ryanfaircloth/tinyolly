{
  "branches": ["main"],
  "tagFormat": "demo-otel-agent-v${version}",
  "plugins": [
    ["@semantic-release/commit-analyzer", { "preset": "conventionalcommits" }],
    ["@semantic-release/release-notes-generator", { "preset": "conventionalcommits" }],
    [
      "@pdecat/semantic-release-python",
      {
        "setupPy": "pyproject.toml",
        "pypiPublish": false
      }
    ],
    [
      "semantic-release-yaml",
      {
        "commands": [
          {
            "cwd": "../../charts/ollyscale-otel-agent",
            "file": "values.yaml",
            "selector": "image.tag",
            "value": "v${nextRelease.version}"
          }
        ]
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": ["pyproject.toml", "../../charts/ollyscale-otel-agent/values.yaml"],
        "message": "chore(demo-otel-agent): release v${nextRelease.version}"
      }
    ],
    "@semantic-release/github"
  ]
}
