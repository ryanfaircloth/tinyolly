{
  "branches": ["main"],
  "tagFormat": "ollyscale-ui-v${version}",
  "plugins": [
    ["@semantic-release/commit-analyzer", { "preset": "conventionalcommits" }],
    ["@semantic-release/release-notes-generator", { "preset": "conventionalcommits" }],
    ["@semantic-release/changelog", { "changelogFile": "CHANGELOG.md" }],
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "../../scripts/update-chart-values.sh ollyscale-ui ${nextRelease.version}",
        "publishCmd": "echo 'ollyscale-ui:${nextRelease.version}'"
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": ["CHANGELOG.md", "package.json", "../../charts/ollyscale/values.yaml"],
        "message": "chore(ollyscale-ui): release v${nextRelease.version}"
      }
    ],
    "@semantic-release/github"
  ]
}
