{
  "branches": ["main"],
  "tagFormat": "demo-v${version}",
  "plugins": [
    ["@semantic-release/commit-analyzer", { "preset": "conventionalcommits" }],
    ["@semantic-release/release-notes-generator", { "preset": "conventionalcommits" }],
    ["@semantic-release/changelog", { "changelogFile": "CHANGELOG.md" }],
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "../../scripts/update-chart-values.sh demo ${nextRelease.version}",
        "publishCmd": "echo 'demo:${nextRelease.version}'"
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": ["CHANGELOG.md", "pyproject.toml", "../../charts/ollyscale-demos/values.yaml"],
        "message": "chore(demo): release v${nextRelease.version}"
      }
    ],
    "@semantic-release/github"
  ]
}
