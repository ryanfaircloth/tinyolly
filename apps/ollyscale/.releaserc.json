{
  "branches": ["main"],
  "tagFormat": "ollyscale-v${version}",
  "plugins": [
    ["@semantic-release/commit-analyzer", { "preset": "conventionalcommits" }],
    ["@semantic-release/release-notes-generator", { "preset": "conventionalcommits" }],
    ["@semantic-release/changelog", { "changelogFile": "CHANGELOG.md" }],
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "../../scripts/update-chart-values.sh ollyscale ${nextRelease.version}",
        "publishCmd": "echo 'ollyscale:${nextRelease.version}'"
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": ["CHANGELOG.md", "pyproject.toml", "../../charts/ollyscale/values.yaml"],
        "message": "chore(ollyscale): release v${nextRelease.version}"
      }
    ],
    "@semantic-release/github"
  ]
}
