{{- if .Values.opampServer.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tinyolly.opampServer.serviceAccountName" . }}
  labels:
    {{- include "tinyolly.opampServer.labels" . | nindent 4 }}
  {{- with .Values.opampServer.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
