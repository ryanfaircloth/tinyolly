apiVersion: v1
kind: Service
metadata:
  name: ai-agent
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "tinyolly-ai-agent.agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.agent.service.type }}
  selector:
    {{- include "tinyolly-ai-agent.agent.selectorLabels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.agent.service.port }}
      targetPort: 8000
      name: http
